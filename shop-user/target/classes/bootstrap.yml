eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: user
  cloud:
    config:
      discovery:
        service-id: CONFIG
        enabled: true
      profile: dev
      label: yang
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
