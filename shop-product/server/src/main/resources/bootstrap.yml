eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        service-id: CONFIG
        enabled: true
      profile: yang
      label: yang
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss